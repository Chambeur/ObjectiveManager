<% @objectives.each do |objective| %>
	<%= link_to "#{objective.title}", objective_path(objective) %> : <%= link_to "Delete", objective_path(objective), :method => "delete", :confirm => "Are you sure?" %>  <br>
<% end %>

<%= link_to "Create new objective", new_objective_path %>